using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class shoufen : MonoBehaviour {

	public Score kk;
	bool isji;
	// Use this for initialization
	void Start () {
		
	}
	
	// Update is called once per frame
	void Update () {
   //     if (GetComponent<EnemyMove>().m_life <= 0)
   //     {
			//if (!isji)
			//{
			//	kk.kk += 10;
			//	isji = true;
			//}
   //     }
	}
	
}
